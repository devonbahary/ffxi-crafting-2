{
  "name": "ffxi-crafting",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "license": "ISC",
  "author": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=packages/api & npm run dev --workspace=packages/client -- --open",
    "discovery": "npm run discovery --workspace=packages/workers",
    "enricher": "npm run enricher --workspace=packages/workers",
    "pricer": "npm run pricer --workspace=packages/workers",
    "profitability": "npm run profitability --workspace=packages/workers",
    "pricer:requeue": "npm run pricer:requeue --workspace=packages/workers --",
    "profitability:requeue": "npm run profitability:requeue --workspace=packages/workers",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint packages",
    "lint:fix": "eslint packages --fix",
    "format": "prettier --write \"packages/**/*.{ts,js,json}\"",
    "format:check": "prettier --check \"packages/**/*.{ts,js,json}\"",
    "prepare": "husky",
    "queue:clear": "docker compose exec db psql -U ${POSTGRES_USER:-ffxi} ${POSTGRES_DB:-ffxi_crafting} -c \"DELETE FROM pgboss.job;\"",
    "db:reset": "docker compose exec db psql -U ${POSTGRES_USER:-ffxi} postgres -c \"DROP DATABASE IF EXISTS ${POSTGRES_DB:-ffxi_crafting};\" && docker compose exec db psql -U ${POSTGRES_USER:-ffxi} postgres -c \"CREATE DATABASE ${POSTGRES_DB:-ffxi_crafting};\" && npm run db:push --workspace=packages/db"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "husky": "^9.1.7",
    "pino-pretty": "^13.1.3",
    "prettier": "^3.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
